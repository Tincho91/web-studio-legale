import { Roboto, Ruda } from "next/font/google";

export const ruda = Ruda({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '700'],
});